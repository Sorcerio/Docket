# {{ title }}

Generated by `docket docs roadmap`. Regenerate it rather than editing it, since the next run overwrites whatever is here.

```mermaid
{{ mermaid }}```

## Legend

An arrow points from what must happen first to what follows.

| Shape | Status |
|---|---|
| `[ ]` | todo, not started |
| `{ }` | wip, in flight |
| `( )` | done |

A node's border carries its priority, heaviest and warmest at 0 and draining to a thin grey at the least urgent. Status is the fill.
{% if hasExternal %}

A dashed border marks a ticket from outside this roadmap's scope, shown only because something inside the scope depends on it or is depended on by it.
{% endif %}
